[[redirects]]
from = "/*"
to = "/.netlify/functions/server"
status = 200
force = true

[build]
command = "npm run build"
publish = "dist/client"
functions = "dist/server"

[context.production.environment]
NODE_ENV = "production"
